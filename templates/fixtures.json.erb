<%# fixtures.json — примеры запросов, ответов и уведомлений.
    Документ целиком собирает Generators::Fixtures::View: ключи там уже в
    нужном порядке, значения — из IR и презентеров сервиса. Шаблон только
    печатает JSON, чтобы стадия оставалась единообразной: один артефакт —
    один templates/*.erb. -%>
<%= json %>
