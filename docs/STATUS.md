# Состояние конвейера

Что уже работает, как это проверить и что делать следующим. Файл для людей и
для агентов: читается после `CLAUDE.md`, обновляется в том же коммите, что
закрывает этап. Всё, что здесь написано, обязано воспроизводиться командой —
иначе это не состояние, а надежда.

Обновлено: 4 сентября 2026, после промпта 4 из `docs/PROMPTS.md`.

---

## Стадии

```
SpecLoader → OverlayApplier → Analyzers → IR → Generators → Validators → Reporter
  готов        нет             4 из 10    готов    нет         нет        Summary
```

| Стадия | Состояние | Точка входа | Проверка |
|---|---|---|---|
| SpecLoader | готов | `SpecGen::SpecLoader.load(path)` | `spec/unit/specgen/spec_loader/` — 18 видов плохого входа дают `SpecGen::Error` с файлом и JSONPath |
| OverlayApplier | не начат | — | слот между `Reader` и `StructureValidator` описан в комментарии `spec_loader/loader.rb` |
| Rules (справочники) | готов, 9 книг | `SpecGen::Rules.load` | `rules/*.yml`; противоречие между книгами валит загрузку одним `RulesError` со всеми проблемами |
| IR | готов | `SpecGen::IR::ProviderProfile` | `docs/IR.md`; `to_h` детерминирован |
| Analyzers, часть 1 | готов: Info, Auth, Operation, Schema | `SpecGen::Analyzers::Runner.call` | `./integrate analyze --spec ...` |
| Analyzers, часть 2 | не начат: Units, Status, Error, Webhook, Idempotency, Conditions | — | промпт 5 |
| Matchers (роли полей) | не начат | — | промпт 6; до него роли всех полей и параметров — `Derived.unknown` намеренно |
| Generators | не начат | — | промпт 7; `./integrate --spec` пока падает с «не реализовано» |
| Mock provider | не начат | — | промпт 8 |
| Validators | не начат | — | промпт 9 |
| Reporter | только консольный `Summary` | `SpecGen::Reporter::Summary` | `report.md` — промпт 7 |

---

## Живая демонстрация на сегодня

```sh
./integrate analyze --spec spec/fixtures/specs/novapay.yaml
./integrate analyze --spec spec/fixtures/specs/novapay.yaml --explain
./integrate analyze --spec spec/fixtures/specs/novapay.yaml --provider novapay
```

Первая команда — экран с провайдером, серверами, авторизацией, пятью
операциями с ролями и уверенностью, восемью схемами с 31 полем и четырьмя
предупреждениями. Вторая добавляет evidence под каждое выведенное значение и
Overlay-фрагмент под каждое исправимое предупреждение. Третья показывает
разницу между `heuristic 0.80` и `structural 1.00`.

Что эта команда доказывает и что нет: она доказывает разбор (критерии
экспертов E1.1 и часть E1.2, техжюри T1.1–T1.3) и провайдер-нейтральность
ядра (E4.2). Она **не** доказывает генерацию — артефактов в `output/` пока нет.

Сценарий первого чекпоинта с таймингом и вопросами экспертам —
`docs/CHECKPOINT-1.md`. Там же девять противоречий выданной спеки, найденных
анализаторами; каждое станет строкой `report.md`.

---

## Что известно о `novapay.yaml` после разбора

Коротко, подробности в `docs/CHECKPOINT-1.md`, раздел 4.

- Пять операций, не шесть. Роли: `create_payout`, `fetch_status`, `cancel`,
  `webhook`, `balance`; две последние вне контракта `BaseService`.
- `409` на `POST /payouts` возвращает `PayoutResponse` — путь дедупликации,
  а на `POST /payouts/{id}/cancel` — `ErrorResponse`. Карту ошибок нельзя
  строить по HTTP-коду, только по коду вместе со схемой ответа.
- Коды `not_found`, `invalid_status`, `unauthorized` есть в примерах, но не в
  enum `PayoutError.code`. Карта ошибок — объединение.
- Условная обязательность `bank_code` / `card_number` есть только в прозе
  описания; вытянута с уверенностью 0.5 и Overlay-фрагментом.
- Подпись вебхука описана словами: алгоритм назван, кодировка и
  подписываемая строка — нет.

---

## Соглашения, установленные в ходе работы над анализаторами

Не выводятся из кода при беглом чтении, поэтому записаны здесь.

**Каждый анализатор** наследует `Analyzers::Base`, получает `document`,
`profile`, `rules`, `options` и заполняет свою часть профиля. Всё выведенное —
`IR::Derived` с `evidence`, которое потом печатает отчёт. `Derived.unknown`
всегда сопровождается `profile.warn`: неизвестное без предупреждения — это
молчаливая дыра.

**Новый код предупреждения** добавляется в `IR::Warning::CODES` осознанно, с
комментарием над константой (комментарии внутри `%i[...]` становятся
символами). Ничего про запас.

**Новый справочник** — это четыре места сразу: файл `rules/<имя>.yml`, класс
`Rules::<Имя>Book < Book`, строка в `Rules::Registry::BOOKS` плюс
`attr_reader`, дефолт в `spec/support/rules_fixtures.rb` и приёмка данных в
`spec/unit/specgen/rules/dictionaries_spec.rb`. Загрузчик книги обязан падать
на противоречиях, а не брать первое совпадение.

**Имена схем** — только через `Analyzers::SchemaNaming`. Компонентная схема
называется именем компонента (по маркеру `x-specgen-ref`, который оставляет
резолвер), инлайновая — `<operationKey>.responses.<status>` или
`<operationKey>.requestBody`. OperationAnalyzer и SchemaAnalyzer обязаны
называть одну схему одинаково; на это есть тест.

**Роли полей и параметров** до появления матчеров — `Derived.unknown` с
evidence «field matchers, a later stage». Не проставлять роли в анализаторах
схем: это рассорит два слоя.

**Пороги и веса** матчера ролей операций — в `rules/operations.yml`, не в
коде. Порог `floor` появился после замера, а не из головы: без него
`POST /things` с телом давал `cancel 3.0 из 3.0` и уверенность 0.95 на
пустом месте. Новые пороги вводить так же — по замеру на фикстурах.

**История коммитов** — по одному коммиту на этап, каждый самодостаточен:
`rspec` и `rubocop` зелёные на каждом. Общие файлы (`analyzers.rb`,
`warning.rb`, `registry.rb`) входят в коммит промежуточным состоянием, а не
финальным. Сообщения на русском, тело объясняет решения, которых не видно в
diff.

---

## Окружение разработки на этой машине

- Ruby 3.4.10 в `C:\Ruby34-x64`. В каждой Git Bash-сессии перед командами:
  `export PATH="/c/Ruby34-x64/bin:$PATH"`.
- Репозиторий LF-only (`.gitattributes`). Ruby в текстовом режиме на Windows
  пишет CRLF: файлы писать в бинарном режиме или через инструменты
  редактирования, после массовых правок проверять `file`.
- Heredoc через Bash-инструмент агента съедает обратные слэши: `\d` доезжает
  как `d`, `\b` — как настоящий backspace. Всё с регулярными выражениями
  писать через Write/Edit, потом проверять
  `grep -rlP '[\x00-\x08\x0b\x0c\x0e-\x1f]' lib/ rules/ spec/`.

---

## Следующий шаг

Промпт 5 из `docs/PROMPTS.md` — анализаторы, часть 2. Сразу за ним промпт 7
(генераторы) и промпт 8 (мок-провайдер): на генерации стоит 25 баллов
экспертов, из них 10 только за живую отправку запроса, и ко второму
чекпоинту эксперты должны увидеть один сгенерированный сервис, а не десять
анализаторов.
