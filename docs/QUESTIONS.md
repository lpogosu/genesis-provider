# Вопросы к экспертам

Открытые вопросы, ответы на которые меняют реализацию. Не выдумываем ответы:
пока вопрос открыт, в коде стоит допущение из `ASSUMPTIONS.md` с пометкой,
что оно ждёт подтверждения.

Правила ведения:

- вопрос формулируется так, чтобы на него можно было ответить одной фразой
- ответ записывается дословно, с датой и чекпоинтом, на котором получен
- после ответа вопрос закрывается, а следствие переносится в
  `ASSUMPTIONS.md` или в справочники `rules/`

| # | Вопрос | Зачем нам ответ | Ответ (дословно) | Дата | Статус |
|---|---|---|---|---|---|
| 1 | `accepted` — это «запрос принят» или «средства зачислены»? | Сейчас синоним `in_progress` в `rules/statuses.yml`; если второе — статус переезжает в `ambiguous`, и мы предупреждаем вместо маппинга | «в файле задания есть Документация интеграции (INTEGRATION.md) — в нём Маппинг статусов — ориентируемся на него.» | 5 сентября 2026, чекпоинт 2 | закрыт косвенно: канон — таблица описания кейса, `accepted` в ней нет, значит это не канонический статус, а слово из словаря синонимов. Остаётся `in_progress` с пометкой источника |
| 2 | Какая арность у `approve_operation` и `reject_operation` и что такое `client`? | В `rules/contract.yml` они помечены `source: допущение`; шаблон сервиса вызывает их вслепую | «Неважно, на самом деле, как он устроен, можно представить, что это какой-то базовый сервис, у которого реализованы методы. Например, вместо createRequest у него NoMethodError, вместо fetchStatus NoMethodError. Можно так представить, что это просто базовый класс-фабрика. Как устроены approve_operation, reject_operation, client — это абсолютно не важно. Это не относится к задаче. Условно, там есть какой-то клиент, где-то там, к которому мы будем обращаться через POST, передавая URL, payload и header.» «Суть задачи в том, чтобы сгенерировать этот сервис по примеру методов, которые написаны.» | 4 сентября 2026, чекпоинт 1 | закрыт: контракт остаётся допущением в `rules/contract.yml`, клиент вызываем как `client.post(url, payload, headers)`; арность хелперов оставляем по примеру из описания кейса |
| 3 | `cancelled` действительно схлопывается в `rejected`, или появится четвёртый внутренний статус? | Канон кейса говорит `rejected`, но отмена логически отдельна; от ответа зависит `IR::Roles::INTERNAL_STATUS` | «в файле задания есть Документация интеграции (INTEGRATION.md) — в нём Маппинг статусов — ориентируемся на него.» | 5 сентября 2026, чекпоинт 2 | закрыт: канон — таблица «Маппинг статусов» описания кейса, `cancelled` → `rejected`, четвёртого внутреннего статуса нет |
| 4 | Подписывает ли кто-то из провайдеров **запросы** (не вебхуки) по HMAC? | В `securitySchemes` это не выражается: сейчас поддержано только через overlay и профиль `hmac_request` в `rules/auth.yml` | | | закрыт замером, см. вопрос 26: да, подписывают — PAYONE объявляет `scheme: payone-hmac-sha256` прямо в `securitySchemes`, bunq подписывает RSA, открытый банкинг Великобритании — JWS |
| 5 | `amount_limit_exceeded` — лимит на одну выплату или суточный лимит провайдера? | В `rules/errors.yml` код идёт в `escalate` (человек решает), потому что по имени не различить; если это лимит одной операции — правильнее `reject`, и шаблон `funds` надо разделить | | | закрыт ответом на вопрос 22: это лимит одной выплаты, правило `operation_limit` в `rules/errors.yml` даёт `reject`; суточная и месячная квота отделены в правило `quota` с `escalate` |
| 6 | Что делать с 402 Payment Required: ждать пополнения баланса или отклонять операции клиентов? | Сейчас `escalate`: платформа сама баланс провайдера не пополнит, а `reject` потеряет корректные операции. От ответа зависит действие в `rules/errors.yml` и текст `INTEGRATION.md` | «в файле задания есть Документация интеграции (INTEGRATION.md) — в нём Маппинг статусов — ориентируемся на него.» — тот же документ задания, таблица «Обработка ошибок»: 402 / insufficient_balance → «retry later» | 5 сентября 2026, чекпоинт 2 | закрыт: 402 и `insufficient_balance` переведены в `retry_backoff` |
| 7 | В вебхуке одновременно `event` (`payout.failed`) и `status` (`failed`) — что первично для `process_callback`, если они разойдутся? | Сейчас статус события выводится из имени события (снятие префикса) и подтверждается полем `status` только в примерах; при расхождении генератор должен выбрать одно и предупредить | «в файле задания есть Документация интеграции (INTEGRATION.md) — в нём Маппинг статусов — ориентируемся на него.» | 5 сентября 2026, чекпоинт 2 | закрыт частично: канон — таблица статусов, поэтому имя события остаётся первичным (это тот же статус с префиксом), а расхождение с полем `status` даёт предупреждение. Дословного ответа именно про расхождение нет |
| 8 | Баланс автоматики: очевидное автоматически, спорное человеку с подсказкой — или инструмент должен решать больше сам? | От ответа зависит третий уровень доверия: оставлять `nil` + TODO или подставлять лучшую догадку + предупреждение | «Да, инструмент должен больше решать сам. То есть понятно, что у разных провайдеров, например, payout называется по-разному. Может там типа быть url-payout, url-withdraw, ещё какие-то варианты. Это можно сделать через эвристические методы, взять популярные все эти слова, какие-то списки их сделать и искать совпадения. То есть он в любом случае должен генерировать класс провайдера и, ну, понятно, там человек уже всё равно всё будет смотреть. Но уже после того, как всё это сгенерировано.» | 4 сентября 2026, чекпоинт 1 | закрыт: генерация не блокируется никогда; на третьем уровне подставляем лучшую догадку из словарей и помечаем её в отчёте и TODO, а не оставляем пусто. См. `ASSUMPTIONS.md` № 3 |
| 9 | На защите проверяют на новой спецификации или только на NovaPay? | Приоритет между глубиной на выданной спеке и устойчивостью к незнакомой | «И да, и нет. Это должно работать с NovaPay, это должно работать с новой спецификацией. То есть вы можете сами поискать в интернете провайдеров и попробовать подумать, как это будет работать с другими провайдерами, [а не] только с примерами с NovaPay.» И раньше: «Провайдер будет вот этот NovaPay, также это должно работать с другими провайдерами, которые можно также найти — Swagger, OpenAPI, в интернете это всё находится в открытом доступе.» | 4 сентября 2026, чекпоинт 1 | закрыт: к своим трём спекам добавляем настоящие публичные OpenAPI платёжных провайдеров и гоняем на них |
| 10 | Считается ли «формирует и отправляет запросы» живой отправкой, нужен ли мок-провайдер? | От ответа зависит приоритет промпта 8 (мок-провайдер) против генераторов | «Разбор спецификации реальных запросов-то и не будет, то есть ваш результат — это не реальные запросы будут к провайдеру, это будет сгенерированный класс провайдера, в котором методы соответствуют методам, и там поля, хидеры и всё такое соответствуют тому, что было в этом YAML-файле.» | 4 сентября 2026, чекпоинт 1 | закрыт: оценивают сгенерированный класс, а не живой трафик. Мок-провайдер понижен до демонстрационного бонуса, приоритет — генераторы и сверка запросов со спекой |
| 11 | Docker обязателен или достаточно `bundle install`? Нужны ли исполняемые тесты помимо `fixtures.json`? | Упаковка и объём тестов | «Докер не обязателен. Достаточно будет исполняемого Ruby-файла и, например, инструкции написать, как это запустить. Тесты нужны на ваше решение, а не на сам сервис.» | 4 сентября 2026, чекпоинт 1 | закрыт: без Docker; RSpec на генератор обязателен, тесты на сгенерированный сервис — не требуются |
| 12 | 409 при повторном создании выплаты: подхватывать существующую выплату или отклонять операцию? | Ветка дедупликации в сгенерированном сервисе | «По идее это не особо относится к сервису провайдера, на него не придёт выплата, которая создана. Это всё отсеется гораздо раньше, на провайдера придёт только выплата, которой до этого не было. Класс провайдера отвечает только за то, чтобы взять операцию и постучаться с ней к провайдеру. А то, что операция валидная, с ней всё в порядке, она не создана раньше с такими же ключами, — это уже всё до этого проверилось.» | 4 сентября 2026, чекпоинт 1 | закрыт: дубли отсекает платформа до вызова сервиса. Ветку 409 → «взять существующую выплату» оставляем как поведение по спеке, но не делаем из неё аргумент на защите |
| 13 | Выражения платформы в `rules/contract.yml` (`operation.recipient_phone`, `recipient_type`, `bank_code`, `card_number`, `provider_operation_id`) — наше допущение. Так у вас и есть? Как сервис сохраняет id провайдера после создания операции? | Весь `build_payload` и `writers` в `rules/contract.yml`, раздел `platform`, построены на этом допущении | «сохранение будет происходить вне сервиса провайдера, сервис провайдера должен сделать запрос к самому провайдеру, через абстрактный client и отдать ответ. сам запрос при этом к провайдеру делать не надо.» | 5 сентября 2026, чекпоинт 2 | закрыт: `writers` и `lookup` в `rules/contract.yml` опустошены, `operation.update` и `Operation.find_by` из сгенерированного кода убраны; чтение полей операции осталось допущением. См. `ASSUMPTIONS.md` № 8 |
| 14 | `process_callback` получает `{ body:, headers: }` или уже разобранный JSON? Если тело уже разобрано, откуда брать сырые байты для подписи? | От ответа зависит, можно ли вообще проверить HMAC по сырому телу так, как это делает `verify_signature!` | «получает уже разобранный JSON внутри payload» | 5 сентября 2026, чекпоинт 2 | закрыт: аргумент — разобранное тело. Проверка подписи вынесена в публичный `verify_webhook_signature(raw_body, headers)` для маршрута вебхука; внутри колбэка подпись проверяется, только если маршрут передал байты. Пункт об этом печатается в чек-листе `report.md`. См. `ASSUMPTIONS.md` № 10 |
| 15 | Нужен ли платформе `ProviderGateway`-конфиг вроде раздела 6 `INTEGRATION.md`, или это лишний придуманный интерфейс? | Форма регистрации сервиса в `INTEGRATION.md` — наш вывод, реального интерфейса шлюза не выдали (см. `ASSUMPTIONS.md` № 9) | «Нет, такая штука не нужна, нужно прямо генерировать класс провайдера. Вот, и там фикстуры и всё, что было сказано в условии задачи. Это какая-то дополнительная вещь, тоже точно не является обязательной.» И письменно: «из open api нового провайдера — должен сформироваться сервис самого провайдера + документация + тестовые фикстуры. адрес, авторизация и т.д. должны браться из open api документа провайдера.» | 5 сентября 2026, чекпоинт 2 | закрыт: раздел 6 перестал предлагать интерфейс и стал сводкой «Параметры подключения», выведенных из спецификации. Допущение № 9 снято |
| 16 | Что важнее к третьему чекпоинту: сводная таблица по чужим спекам или цикл `broken.yaml` → отчёт → `--fix` → повторный прогон? | Оба пункта в промпте 10/11, но не факт, что успеем оба одинаково глубоко | «Ну, наверное, важнее всё-таки чужие OpenAPI-файлы уметь парсить и что-то из них всё-таки доставать, чем вот этот overlay-цикл делать.» | 5 сентября 2026, чекпоинт 2 | закрыт: приоритет — покрытие чужих спецификаций; `--fix` уходит в конец очереди |
| 17 | Считается ли содержимое `output/` при подсчёте доли Ruby? | Открытый вопрос из `CLAUDE.md`; веб-интерфейс тоже считается полностью, поэтому граница важна | «сама реализация должна быть написана на ruby, а не на другом языке» | 5 сентября 2026, чекпоинт 2 | закрыт по существу: границу `output/` жюри не проводит, важно, что инструмент написан на Ruby. Веб-интерфейс — по-прежнему тонкий Rack + ERB без npm |
| 18 | Какие поля гарантированы у объекта `operation`? В примере из описания кейса реквизиты читаются как `operation.payout_requisite.dig('sbp', 'phone')`, а мы генерируем плоские `operation.recipient_phone`, `bank_code`, `card_number` — одно из двух неверно | На этом построен весь `build_payload` и таблица `accessors` в `rules/contract.yml`; ошибка здесь означает, что сгенерированный сервис не работает вообще | «Для кейса гарантированно ориентироваться на: `operation.id`, `operation.amount`, `operation.payout_requisite` — JSONB-хеш, не плоские `recipient_phone` / `bank_code`. Типичные формы реквизитов: СБП: `payout_requisite.dig('sbp', 'phone')`, `…('sbp', 'bank_code')`, `…('sbp', 'bank_name')`; карта: часто `payout_requisite['card_number']` (или вложенная card-структура). Отдельных полей `recipient_phone` / `bank_code` на операции нет — всё из `payout_requisite` (+ при необходимости `gateway`, `provider_operation_key`, `status`).» | 5 сентября 2026, чат | закрыт: плоские выражения из `rules/contract.yml` убраны, реквизиты читаются из `payout_requisite`. Право оказалось описание кейса, а не наша таблица. Допущение № 8 снято |
| 19 | Что возвращает `create_request` — `success` без аргументов, а платформа сама разберёт ответ клиента, или результат обязан нести тело ответа провайдера? | От этого зависит, как сервис отдаёт идентификатор операции у провайдера, который платформа сохраняет снаружи (вопрос 13) | «Не «голый» `success` без данных. Сервис сам разбирает ответ провайдера и возвращает payload, из которого платформа забирает ID: `success(result: { id: "np_7f3a9b2c" })`, опционально ещё `rate` и т.п. Платформа после успеха делает примерно: `provider_operation_key = payload.dig(:result, :id)`. Сырой HTTP-ответ клиенту наружу не обязателен — важен нормализованный `result[:id]`.» | 5 сентября 2026, чат | закрыт: `create_request` возвращает `success(result: { id: … })`, выражение читает идентификатор из разобранного тела по роли `provider_operation_id` |
| 20 | У `failure(:code, 'i18n.key')` первый аргумент — фиксированный набор кодов платформы или произвольный символ на усмотрение сервиса? | Сейчас первым аргументом уходит наше внутреннее действие (`retry_backoff`, `escalate`) и придуманные коды вроде `:status_unknown` | «Первый аргумент — символ в духе HTTP/доменного кода платформы, не совсем произвольный набор: часто `:bad_request`, `:unauthorized`, `:unprocessable_entity`, `:too_many_requests`, `:internal_server_error`, `:forbidden`… Второй — обычно i18n-ключ или строка ошибки (`'operation.provider_error'`, `'provider.rate_limit'`…). Для скелета: маппить HTTP провайдера → эти коды (как в README: 401 → unauthorized, 429 → too_many_requests). Выдумывать уникальные `:novapay_whatever` не нужно.» | 5 сентября 2026, чат | закрыт: коды платформы вынесены в `failure_codes` в `rules/contract.yml` и считаются по HTTP-коду ответа; смысл отказа остаётся вторым аргументом в ключе локализации. Наше действие никуда не делось — оно в `ERROR_MAP`, `RETRY_POLICY` и таблице `INTEGRATION.md` |
| 21 | Нужно ли в `create_request` ветвление по `request_method`, когда провайдер поддерживает несколько способов выплаты (СБП, карта)? | Условная обязательность из спеки (`bank_code` при `type=sbp`) сейчас становится только TODO-комментарием, а в payload валится всё сразу | «`request_method` на платформе — логический метод шлюза (`gateway.payment_method`: `sbp`, `p2p`, …), не HTTP. Если у провайдера СБП и карта: на платформе обычно разные gateway / `payment_method`; в одном сервисе — ветки по `request_method` и/или форме `payout_requisite` (`type: sbp` vs `card`). Оба подхода ок; для NovaPay логично: один сервис, внутри `create_request` — ветка по типу получателя / `request_method`.» | 5 сентября 2026, чат | закрыт: условная обязательность превращается в ветку `case request_method` внутри сборки реквизитов. Разбор спеки наконец даёт рабочий код, а не комментарий |
| 22 | `amount_limit_exceeded` у NovaPay — лимит на одну выплату или суточный лимит провайдера? (повтор вопроса 5) | От ответа зависит действие в `rules/errors.yml`: отклонять операцию или отдавать её человеку | «В спеке не зафиксировано «разовая vs суточная». Для хакатона канон: считать лимитом на эту выплату (сумма вне допустимого диапазона / max на операцию) → отклонять операцию (rejected / validation), не «отдавать человеку». Суточный лимит ближе к другим сценариям (`insufficient_balance` / свои daily-лимиты на стороне оркестратора). Если команда трактует иначе — явно описать в INTEGRATION.md.» | 5 сентября 2026, чат | закрыт: правило `limits` разделено на `operation_limit` (лимит одной операции → `reject`) и `quota` (суточная и месячная квота → `escalate`). Вопрос 5 закрыт этим же ответом |
| 23 | Карта: брать плоский ключ `payout_requisite['card_number']` или рассчитывать на `payout_requisite.dig('card', 'number')`? | Единственное место, где иначе пришлось бы оставить заглушку вместо готового кода | «берите плоский ключ» | 5 сентября 2026, чат | закрыт: `operation.payout_requisite['card_number']` в таблице `requisites` в `rules/contract.yml` |
| 24 | У `fetch_status` и `process_callback` тоже `success(result: {...})` со статусом, или достаточно простого `success`, а статус платформа берёт из `approve_operation` / `reject_operation`? | Форма возврата трёх методов контракта из четырёх | «отдельный `success(result: { status: … })` для смены статуса платформе не нужен. Статус операции меняют хелперы апрува/реджекта» | 5 сентября 2026, чат | закрыт: `result` несёт только `create_request`. `fetch_status` и `process_callback` возвращают голый `success` после вызова хелпера |
| 25 | Для нового провайдера, у которого реквизиты не СБП и не карта (IBAN, номер счёта, налоговый идентификатор): ключи внутри `payout_requisite` совпадают с именами полей провайдера как есть, или платформа складывает их по своей схеме? | От этого зависит, можем ли мы для незнакомого обязательного поля сразу генерировать `payout_requisite['имя_поля']` или обязаны оставлять TODO | «Для поля из спеки, которого нет в известной схеме платформы, не генерировать вслепую. Лучше TODO и явное место маппинга в INTEGRATION.md» | 5 сентября 2026, чат | закрыт, и это подтверждает третий уровень доверия из `CLAUDE.md`: догадка о ПЛАТФОРМЕ запрещена, догадка о СПЕЦИФИКАЦИИ обязательна. Роли без выражения в `requisites` дают TODO и строку «куда мапить» в `INTEGRATION.md`; новые роли полей (`account_number`, `iban`, `recipient_name`) по-прежнему не заводим |
| 26 | Подписывает ли кто-нибудь из провайдеров сами ЗАПРОСЫ (не вебхуки) по HMAC? Единственный вопрос, остававшийся открытым в `CLAUDE.md` | От ответа зависит, нужен ли профиль `hmac_request` в `rules/auth.yml` или он гипотетический | Отвечен не экспертом, а замером: **да, подписывают, и тремя разными способами**. PAYONE Link объявляет схему прямо в `components.securitySchemes` — `type: http`, `scheme: payone-hmac-sha256`, три записи (`createAuth`, `getMultipleAuth`, `getSingleAuth`) с описанием «Base64 encoded (…) signed with Portalkey in HmacSHA256»; bunq подписывает RSA-SHA256 и запрос, и ответ (`X-Bunq-Client-Signature`, `X-Bunq-Server-Signature`); открытый банкинг Великобритании — отсоединённой подписью JWS по RFC 7515 (заголовок `x-jws-signature` у самих операций) | 6 сентября 2026, замер по корпусу из 33 публичных спецификаций (3177 операций) | закрыт: профиль `hmac_request` не гипотетический. В `rules/auth.yml` добавлена запись `hmac_request_payone` с реальным написанием `scheme`; RSA и JWS распознаются как подпись (их заголовки в `header_names` `rules/signatures.yml` → роль `signature` и строка в отчёте), но кода не генерируют: в `IR::Auth::TYPES` нет значений для RSA и JWS, а выдумывать тип модели в справочнике запрещено. Обобщить `scheme`, содержащий `hmac`, одним правилом нельзя без ключа-шаблона в `AuthBook` — заявка владельцу `lib/specgen/rules/auth_book.rb` |

Отдельная заметка с чекпоинта 1: аргумент «один вход всегда даёт один
результат, это доказывает, что нейросети внутри нет» экспертов озадачил
(«К чему это было?»). Про запрет нейросетей никто не спрашивал — на защите
этот тезис не поднимаем, детерминированность упоминаем только как свойство
инструмента, если спросят про тесты.

Заметки с чекпоинта 2 (5 сентября 2026), дословно:

- о процессе: «Да нет, это достаточно последовательный, понятный процесс» —
  вопрос «ждёте ли чего-то между командой и результатом» закрыт, менять CLI
  ради понятности больше не нужно;
- о живом прогоне: «Окей, то есть вы это всё завернули в презентацию?» —
  на третьем чекпоинте показываем терминал вживую, а не скриншот в слайде;
- просьба, которую надо выполнить: «Вот было бы интересно на этот файлик,
  конечно, посмотреть» (про сгенерированный сервис по чужой спецификации) и
  «если у вас ещё есть, кроме презентации, там на гите какой-то репозиторий,
  можете через куратора скидывать, мы их посмотрим — и презентацию, и код».
  Значит эксперты будут читать сгенерированный файл и репозиторий глазами.
