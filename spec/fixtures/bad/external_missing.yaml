openapi: 3.0.3
info:
  title: Missing external file
  version: 1.0.0
paths:
  /payouts:
    post:
      requestBody:
        content:
          application/json:
            schema:
              $ref: 'nowhere.yaml#/components/schemas/Money'
      responses:
        '201':
          description: created
